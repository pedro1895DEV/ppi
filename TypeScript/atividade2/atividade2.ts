let sabor: string;
sabor = 'chocolate';

function escreveSabor(sab: string) {
    console.log(`Pizza sabor ${sab}`);
}

escreveSabor(sabor);

let registrado: number;
registrado = 1;
console.log(`${registrado ? 'Usuário registrado' : 'Usuário não registrado'}`);